import { Card } from "@repo/ui/card";
import { Button} from "@repo/ui/button"

export default function Page(): JSX.Element {
  return (
    <main>
      <p className="text-2xl p-6">This is the web</p>
      <Button/>
    </main>
  );
}
